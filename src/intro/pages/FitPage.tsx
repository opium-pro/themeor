import React from 'react'
import {Font} from '../../themeor'

interface _PageProps {}
interface _PageState {}

export default class _Page extends React.PureComponent<_PageProps, _PageState> {
  render() {
    return (<>
      Fit component description
    </>)
  }
}